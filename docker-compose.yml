services:
  backend:
    build: ./backend
    ports:
      - 8000:8000
    volumes:
      - ./backend/database:/app/data
      - ./backend/images:/app/images
    runtime: nvidia
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]

  frontend:
    build: ./frontend
    ports:
      - 5173:80
    depends_on:
      - backend